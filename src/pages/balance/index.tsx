import { FunctionComponent } from 'react';
import { Overlay } from 'src/shared';

const BalancePage: FunctionComponent = () => {
    return <Overlay>Balance</Overlay>;
};

export default BalancePage;
